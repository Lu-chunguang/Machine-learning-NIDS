# Component 3: Robustness Strategies

Improves robustness using open-set rejection, Isolation Forest fallback, cost-sensitive learning, few-shot calibration, and feature augmentation.

Useful commands:

```bash
python src/main.py --section open_set
python src/main.py --section cross_dataset
python src/main.py --section feature_robustness
```
