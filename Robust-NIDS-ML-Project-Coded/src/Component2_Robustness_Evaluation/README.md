# Component 2: Robustness Evaluation / Stress Tests

Evaluates the baseline model under held-out attacks, cross-dataset shift, and feature degradation.

Useful commands:

```bash
python src/main.py --section c1+c2a
python src/main.py --section c1+c2b
python src/main.py --section c1+c2c
```
